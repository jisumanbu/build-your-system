{
  "description": "Personal Assistant hooks for context loading and session management",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/load-context.sh",
            "timeout": 15
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "会话即将结束，执行静默整合：\n1. 检查对话中未保存的内容，按 YOLO 模式捕获到 inbox\n2. 如发现关于用户的新洞察，追加到 inbox 待 review\n3. 如果是周日，提醒用户执行 /a-weekly\n\n静默执行，只在有重要事项时简短告知用户。",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
